/**
 * @refNo 1
 * @description cleanup suggestions and todos for getCSSAndLines function
 * @fixme formatting is lost with this trim that makes the array more searchable
 * @todo suggest using find to search instead
 * @alt to keep current compatibility simply have a mirror array and use the founf index on that mirror array     
 */